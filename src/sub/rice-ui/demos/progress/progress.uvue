<template>
  <view class="page-container bg-[#f8fafc] min-h-screen pb-30px">
    <NavBar title="Progress 进度条" :auto-back="true" :safe-area-inset-top="true" bg-color="#ffffff"></NavBar>
    <view class="p-16px">
      <!-- 基础进度条 -->
      <view class="demo-block">
        <text class="demo-label" :style="{ borderLeftColor: currentTheme }">基础用法</text>
        <view class="py-10px">
          <rice-progress :percentage="50" :color="currentTheme" />
        </view>
      </view>

      <!-- 线条粗细与颜色 -->
      <view class="demo-block">
        <text class="demo-label" :style="{ borderLeftColor: currentTheme }">线条粗细与主题色</text>
        <view class="py-10px">
          <view class="mb-14px">
            <rice-progress :percentage="75" stroke-width="8px" :color="currentTheme" />
          </view>
          <view class="mb-14px">
            <rice-progress :percentage="90" stroke-width="12px" color="#ee0a24" />
          </view>
          <view>
            <rice-progress :percentage="60" color="linear-gradient(to right, #ff6034, #ee0a24)" />
          </view>
        </view>
      </view>
    </view>
  </view>
</template>

<script setup lang="uts">
import { useAppStore } from '@/src/store'
import { computed } from 'vue'

const appStore = useAppStore()
const currentTheme = computed((): string => appStore.state.theme)
</script>

<style scoped>
.demo-block {
  margin-bottom: 12px;
  padding: 14px;
  background-color: #ffffff;
  border-radius: 10px;
}
.demo-label {
  font-size: 14px;
  font-weight: bold;
  color: #334155;
  margin-bottom: 12px;
  padding-left: 6px;
  border-left-width: 3px;
  border-left-style: solid;
}
</style>
