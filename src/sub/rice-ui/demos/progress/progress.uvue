<template>
  <view class="page-container bg-[#f8fafc] min-h-screen pb-[30px]">
    <NavBar title="Progress 进度条" />
    <view class="p-[16px]">
      <!-- 基础进度条 -->
      <view class="mb-[12px] p-[14px] bg-white rounded-[10px]">
        <view class="flex-row items-center mb-[12px]">
          <view class="w-[3px] h-[14px] rounded-[1px] mr-[6px]" :style="{ backgroundColor: currentTheme }" />
          <text class="text-[14px] font-bold text-[#334155]">基础用法</text>
        </view>
        <view class="py-[10px]">
          <rice-progress :percentage="50" :color="currentTheme" />
        </view>
      </view>

      <!-- 线条粗细与颜色 -->
      <view class="mb-[12px] p-[14px] bg-white rounded-[10px]">
        <view class="flex-row items-center mb-[12px]">
          <view class="w-[3px] h-[14px] rounded-[1px] mr-[6px]" :style="{ backgroundColor: currentTheme }" />
          <text class="text-[14px] font-bold text-[#334155]">线条粗细与主题色</text>
        </view>
        <view class="py-[10px]">
          <view class="mb-[14px]">
            <rice-progress :percentage="75" stroke-width="8px" :color="currentTheme" />
          </view>
          <view class="mb-[14px]">
            <rice-progress :percentage="90" stroke-width="12px" color="#ee0a24" />
          </view>
          <view>
            <rice-progress :percentage="60" color="linear-gradient(to right, #ff6034, #ee0a24)" />
          </view>
        </view>
      </view>
    </view>
  </view>
</template>

<script setup lang="uts">
import { useAppStore } from '@/src/store';
import { computed } from 'vue';

definePage({
  style: {
    navigationBarTitleText: 'Progress 进度条',
    navigationStyle: 'custom'
  }
});

const appStore = useAppStore();
const currentTheme = computed((): string => appStore.state.theme);
</script>
