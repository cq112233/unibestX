<template>
  <view class="page-container bg-[#f8fafc] min-h-screen pb-[30px]">
    <NavBar title="Progress 进度条" :auto-back="true" :safe-area-inset-top="true" bg-color="#ffffff"></NavBar>
    <view class="p-[16px]">
      <!-- 基础进度条 -->
      <view class="mb-[12px] p-[14px] bg-white rounded-[10px]">
        <text class="text-[14px] font-bold text-[#334155] mb-[12px] pl-[6px] border-l-[3px] border-solid" :style="{ borderLeftColor: currentTheme }">基础用法</text>
        <view class="py-[10px]">
          <rice-progress :percentage="50" :color="currentTheme" />
        </view>
      </view>

      <!-- 线条粗细与颜色 -->
      <view class="mb-[12px] p-[14px] bg-white rounded-[10px]">
        <text class="text-[14px] font-bold text-[#334155] mb-[12px] pl-[6px] border-l-[3px] border-solid" :style="{ borderLeftColor: currentTheme }">线条粗细与主题色</text>
        <view class="py-[10px]">
          <view class="mb-[14px]">
            <rice-progress :percentage="75" stroke-width="8px" :color="currentTheme" />
          </view>
          <view class="mb-[14px]">
            <rice-progress :percentage="90" stroke-width="12px" color="#ee0a24" />
          </view>
          <view>
            <rice-progress :percentage="60" color="linear-gradient(to right, #ff6034, #ee0a24)" />
          </view>
        </view>
      </view>
    </view>
  </view>
</template>

<script setup lang="uts">
import { useAppStore } from '@/src/store';
import { computed } from 'vue';

const appStore = useAppStore();
const currentTheme = computed((): string => appStore.state.theme);
</script>

