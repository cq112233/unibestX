<template>
  <view class="page-container bg-[#f8fafc] min-h-screen pb-[30px]">
    <NavBar title="Collapse 折叠面板" />
    <view class="p-[16px]">
      <!-- 基础折叠面板 -->
      <view class="mb-[12px] p-[14px] bg-white rounded-[10px]">
        <view class="flex-row items-center mb-[12px]">
          <view class="w-[3px] h-[14px] rounded-[1px] mr-[6px]" :style="{ backgroundColor: currentTheme }" />
          <text class="text-[14px] font-bold text-[#334155]">基础用法</text>
        </view>
        <rice-collapse v-model="activeNames">
          <rice-collapse-item title="什么是 Rice UI？" name="1">
            <text class="text-[13px] text-[#64748b] leading-[20px]">
              Rice UI 是一款专为 uni-app X 打造的高性能跨端组件库，基于 Vue 3 组合式 API 开发，兼容 Android、iOS、鸿蒙Next、微信小程序和 H5。
            </text>
          </rice-collapse-item>
          <rice-collapse-item title="是否支持蒸汽模式 (Vapor Mode)？" name="2">
            <text class="text-[13px] text-[#64748b] leading-[20px]">
              是的！Rice UI 全面支持蒸汽模式（Vapor Mode），提供更轻量的包体积与极速的原生渲染体验。
            </text>
          </rice-collapse-item>
          <rice-collapse-item title="如何使用暗黑模式？" name="3">
            <text class="text-[13px] text-[#64748b] leading-[20px]">
              可以通过 setTheme('dark' | 'light') 快速切换组件库主题，并自动适配 CSS 变量。
            </text>
          </rice-collapse-item>
        </rice-collapse>
      </view>
    </view>
  </view>
</template>

<script setup lang="uts">
import { useAppStore } from '@/src/store';
import { computed } from 'vue';

definePage({
  style: {
    navigationBarTitleText: 'Collapse 折叠面板',
    navigationStyle: 'custom'
  }
});

const appStore = useAppStore();
const currentTheme = computed((): string => appStore.state.theme);

const activeNames = ref<string[]>(['1']);
</script>
