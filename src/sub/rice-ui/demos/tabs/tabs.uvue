<template>
  <view class="page-container bg-[#f8fafc] min-h-screen pb-[30px]">
    <NavBar title="Tabs 选项卡" />
    <view class="p-[16px]">
      <!-- 基础选项卡 -->
      <view class="mb-[12px] p-[14px] bg-white rounded-[10px]">
        <view class="flex-row items-center mb-[12px]">
          <view class="w-[3px] h-[14px] rounded-[1px] mr-[6px]" :style="{ backgroundColor: currentTheme }" />
          <text class="text-[14px] font-bold text-[#334155]">基础选项卡</text>
        </view>
        <rice-tabs v-model="tabIndex1" :list="tabList1" :color="currentTheme" />
        <view class="p-[16px] mt-[8px] bg-[#f8fafc] rounded-[8px]">
          <text class="text-[14px] text-[#334155]">当前标签内容：{{ tabList1[tabIndex1].name }}</text>
        </view>
      </view>

      <!-- 多个可滚动标签 -->
      <view class="mb-[12px] p-[14px] bg-white rounded-[10px]">
        <view class="flex-row items-center mb-[12px]">
          <view class="w-[3px] h-[14px] rounded-[1px] mr-[6px]" :style="{ backgroundColor: currentTheme }" />
          <text class="text-[14px] font-bold text-[#334155]">多标签横向滑动</text>
        </view>
        <rice-tabs v-model="tabIndex2" :list="tabList2" :color="currentTheme" />
        <view class="p-[16px] mt-[8px] bg-[#f8fafc] rounded-[8px]">
          <text class="text-[14px] text-[#334155]">当前标签内容：{{ tabList2[tabIndex2].name }}</text>
        </view>
      </view>
    </view>
  </view>
</template>

<script setup lang="uts">
import { useAppStore } from '@/src/store';
import type { TabsOptions } from '@/uni_modules/rice-ui';
import { computed } from 'vue';

definePage({
  style: {
    navigationBarTitleText: 'Tabs 选项卡',
    navigationStyle: 'custom'
  }
});

const appStore = useAppStore();
const currentTheme = computed((): string => appStore.state.theme);

const tabIndex1 = ref(0);
const tabIndex2 = ref(0);

const tabList1: TabsOptions[] = [
  { name: '关注' } as TabsOptions,
  { name: '推荐' } as TabsOptions,
  { name: '热榜' } as TabsOptions
];

const tabList2: TabsOptions[] = [
  { name: '综合' } as TabsOptions,
  { name: '前端' } as TabsOptions,
  { name: 'Android' } as TabsOptions,
  { name: 'iOS' } as TabsOptions,
  { name: 'Harmony' } as TabsOptions,
  { name: 'AI' } as TabsOptions,
  { name: '工具' } as TabsOptions
];
</script>
