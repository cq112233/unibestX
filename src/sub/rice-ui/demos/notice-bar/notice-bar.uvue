<template>
  <view class="page-container bg-[#f8fafc] min-h-screen pb-[30px]">
    <NavBar title="NoticeBar 公告栏" />
    <view class="p-[16px]">
      <!-- 基础滚动公告 -->
      <view class="mb-[12px] p-[14px] bg-white rounded-[10px]">
        <view class="flex-row items-center mb-[12px]">
          <view class="w-[3px] h-[14px] rounded-[1px] mr-[6px]" :style="{ backgroundColor: currentTheme }" />
          <text class="text-[14px] font-bold text-[#334155]">基础滚动公告</text>
        </view>
        <rice-notice-bar
          message="🎉 欢迎体验 unibestX 集成的 Rice UI 组件库，全端原生渲染，轻盈灵动！"
          :scrollable="true"
          :color="currentTheme"
          left-icon="volume"
        />
      </view>

      <!-- 样式类型 -->
      <view class="mb-[12px] p-[14px] bg-white rounded-[10px]">
        <view class="flex-row items-center mb-[12px]">
          <view class="w-[3px] h-[14px] rounded-[1px] mr-[6px]" :style="{ backgroundColor: currentTheme }" />
          <text class="text-[14px] font-bold text-[#334155]">类型与通知</text>
        </view>
        <view class="mb-[10px]">
          <rice-notice-bar type="primary" :color="currentTheme" message="系统已升级至最新版本，体验更佳。" />
        </view>
        <view class="mb-[10px]">
          <rice-notice-bar type="success" message="您提交的申请已审批通过。" />
        </view>
        <view class="mb-[10px]">
          <rice-notice-bar type="error" message="网络连接异常，请检查网络设置。" :closeable="true" />
        </view>
      </view>
    </view>
  </view>
</template>

<script setup lang="uts">
import { useAppStore } from '@/src/store';
import { computed } from 'vue';

definePage({
  style: {
    navigationBarTitleText: 'NoticeBar 公告栏',
    navigationStyle: 'custom'
  }
});

const appStore = useAppStore();
const currentTheme = computed((): string => appStore.state.theme);
</script>
