<template>
  <view class="page-container bg-[#f8fafc] min-h-screen pb-30px">
    <NavBar title="ProgressCircle 圆环进度" :auto-back="true" :safe-area-inset-top="true" bg-color="#ffffff"></NavBar>
    <view class="p-16px">
      <!-- 基础圆环进度条 -->
      <view class="demo-block">
        <text class="demo-label" :style="{ borderLeftColor: currentTheme }">圆环进度条</text>
        <view class="flex-row items-center justify-around py-10px">
          <view class="flex-col items-center">
            <rice-progress-circle :percentage="65" :color="currentTheme" />
            <text class="text-12px text-[#64748b] mt-6px">65%</text>
          </view>
          <view class="flex-col items-center">
            <rice-progress-circle :percentage="80" color="#07c160" />
            <text class="text-12px text-[#64748b] mt-6px">80%</text>
          </view>
          <view class="flex-col items-center">
            <rice-progress-circle :percentage="100" color="#e6a23c" />
            <text class="text-12px text-[#64748b] mt-6px">100%</text>
          </view>
        </view>
      </view>
    </view>
  </view>
</template>

<script setup lang="uts">
import { useAppStore } from '@/src/store'
import { computed } from 'vue'

const appStore = useAppStore()
const currentTheme = computed((): string => appStore.state.theme)
</script>

<style scoped>
.demo-block {
  margin-bottom: 12px;
  padding: 14px;
  background-color: #ffffff;
  border-radius: 10px;
}
.demo-label {
  font-size: 14px;
  font-weight: bold;
  color: #334155;
  margin-bottom: 12px;
  padding-left: 6px;
  border-left-width: 3px;
  border-left-style: solid;
}
</style>
