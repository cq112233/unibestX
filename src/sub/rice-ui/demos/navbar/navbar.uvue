<template>
  <view class="page-container bg-[#f8fafc] min-h-screen pb-[30px]">
    <NavBar title="Navbar 导航栏" />
    <view class="p-[16px]">
      <!-- 基础导航栏 -->
      <view class="mb-[12px] p-[14px] bg-white rounded-[10px]">
        <view class="flex-row items-center mb-[12px]">
          <view class="w-[3px] h-[14px] rounded-[1px] mr-[6px]" :style="{ backgroundColor: currentTheme }" />
          <text class="text-[14px] font-bold text-[#334155]">基础导航栏</text>
        </view>
        <view class="rounded-[8px] overflow-hidden border border-solid border-[#e2e8f0]">
          <rice-navbar title="标题" :left-arrow="true" left-text="返回" :fixed="false" />
        </view>
      </view>

      <!-- 带右侧按钮导航栏 -->
      <view class="mb-[12px] p-[14px] bg-white rounded-[10px]">
        <view class="flex-row items-center mb-[12px]">
          <view class="w-[3px] h-[14px] rounded-[1px] mr-[6px]" :style="{ backgroundColor: currentTheme }" />
          <text class="text-[14px] font-bold text-[#334155]">带右侧操作</text>
        </view>
        <view class="rounded-[8px] overflow-hidden border border-solid border-[#e2e8f0]">
          <rice-navbar
            title="商品详情"
            :left-arrow="true"
            right-text="分享"
            :fixed="false"
            @click-right="onClickRight"
          />
        </view>
      </view>

      <!-- 自定义背景导航栏 -->
      <view class="mb-[12px] p-[14px] bg-white rounded-[10px]">
        <view class="flex-row items-center mb-[12px]">
          <view class="w-[3px] h-[14px] rounded-[1px] mr-[6px]" :style="{ backgroundColor: currentTheme }" />
          <text class="text-[14px] font-bold text-[#334155]">自定义背景与颜色</text>
        </view>
        <view class="rounded-[8px] overflow-hidden border border-solid border-[#e2e8f0]">
          <rice-navbar
            title="品牌主页"
            :left-arrow="true"
            arrow-color="#ffffff"
            :bg-color="currentTheme"
            :fixed="false"
          >
            <template #title>
              <text class="text-white font-bold text-[16px]">品牌主页</text>
            </template>
          </rice-navbar>
        </view>
      </view>
    </view>
  </view>
</template>

<script setup lang="uts">
import { useAppStore } from '@/src/store';
import { computed } from 'vue';

definePage({
  style: {
    navigationBarTitleText: 'Navbar 导航栏',
    navigationStyle: 'custom'
  }
});

const appStore = useAppStore();
const currentTheme = computed((): string => appStore.state.theme);

function onClickRight() {
  uni.showToast({
    title: '点击了右侧分享',
    icon: 'none'
  });
}
</script>
