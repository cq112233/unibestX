<template>
  <view class="page-container bg-[#f8fafc] min-h-screen pb-30px">
    <NavBar title="Badge 徽标" :auto-back="true" :safe-area-inset-top="true" bg-color="#ffffff"></NavBar>
    <view class="p-16px">
      <!-- 基础徽标 -->
      <view class="demo-block">
        <text class="demo-label" :style="{ borderLeftColor: currentTheme }">基础数字徽标</text>
        <view class="flex-row items-center justify-around py-10px">
          <rice-badge :value="5">
            <view class="w-48px h-48px bg-[#e2e8f0] rounded-8px flex items-center justify-center">
              <rice-icon name="chat" size="24px" color="#475569" />
            </view>
          </rice-badge>
          <rice-badge :value="99" max="99">
            <view class="w-48px h-48px bg-[#e2e8f0] rounded-8px flex items-center justify-center">
              <rice-icon name="volume" size="24px" color="#475569" />
            </view>
          </rice-badge>
          <rice-badge :is-dot="true">
            <view class="w-48px h-48px bg-[#e2e8f0] rounded-8px flex items-center justify-center">
              <rice-icon name="user" size="24px" color="#475569" />
            </view>
          </rice-badge>
        </view>
      </view>

      <!-- 自定义类型与独立徽标 -->
      <view class="demo-block">
        <text class="demo-label" :style="{ borderLeftColor: currentTheme }">类型与独立徽标</text>
        <view class="flex-row items-center justify-around py-10px">
          <rice-badge value="HOT" type="error" :single="true" />
          <rice-badge value="NEW" type="success" :single="true" />
          <rice-badge value="VIP" type="warning" :single="true" />
          <rice-badge :value="12" :color="currentTheme" :single="true" />
        </view>
      </view>
    </view>
  </view>
</template>

<script setup lang="uts">
import { useAppStore } from '@/src/store'
import { computed } from 'vue'

const appStore = useAppStore()
const currentTheme = computed((): string => appStore.state.theme)
</script>

<style scoped>
.demo-block {
  margin-bottom: 12px;
  padding: 14px;
  background-color: #ffffff;
  border-radius: 10px;
}
.demo-label {
  font-size: 14px;
  font-weight: bold;
  color: #334155;
  margin-bottom: 12px;
  padding-left: 6px;
  border-left-width: 3px;
  border-left-style: solid;
}
</style>
