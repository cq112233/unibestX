<template>
  <view class="page-container bg-[#f8fafc] min-h-screen pb-[30px]">
    <NavBar title="up-overlay 遮罩层" :auto-back="true" :safe-area-inset-top="true" bg-color="#ffffff"></NavBar>
    <view class="p-[16px]">
      <view class="demo-block">
        <text class="demo-label">基础用法</text>
        <view class="flex-row items-center">
          <UpButton type="primary" text="显示遮罩" size="mini" @click="overlayShow = true"></UpButton>
          <view class="w-[10px]"></view>
          <UpButton type="warning" text="半透明遮罩" size="mini" @click="overlayOpacity = 0.3; overlayShow = true"></UpButton>
        </view>
      </view>
    </view>
    <UpOverlay :show="overlayShow" :opacity="overlayOpacity" @click="overlayShow = false">
      <view class="flex-1 items-center justify-center">
        <view class="bg-white rounded-[8px] p-[20px] items-center" @click.stop="">
          <UpLoadingIcon mode="circle"></UpLoadingIcon>
          <text class="text-[14px] text-[#303133] mt-[10px]">加载中，点击遮罩关闭</text>
        </view>
      </view>
    </UpOverlay>
  </view>
</template>

<script setup lang="uts">
import { ref } from 'vue';
import NavBar from '@/src/components/NavBar/NavBar.uvue';
import UpButton from '@/uni_modules/uview-ultra/components/up-button/up-button.uvue';
import UpOverlay from '@/uni_modules/uview-ultra/components/up-overlay/up-overlay.uvue';
import UpLoadingIcon from '@/uni_modules/uview-ultra/components/up-loading-icon/up-loading-icon.uvue';

definePage({
  style: {
    navigationStyle: 'custom',
    navigationBarTitleText: 'up-overlay 遮罩层'
  }
});

const overlayShow = ref(false);
const overlayOpacity = ref(0.5);
</script>

<style lang="scss" scoped>
.demo-block {
  margin-bottom: 12px;
  padding: 12px;
  background-color: #ffffff;
  border-radius: 8px;
}
.demo-label {
  font-size: 14px;
  font-weight: bold;
  color: #606266;
  margin-bottom: 10px;
  padding-left: 4px;
  border-left-width: 3px;
  border-left-style: solid;
  border-left-color: $up-primary;
}
</style>
