<template>
  <view class="page-container bg-[#f8fafc] min-h-screen pb-[30px]">
    <NavBar title="LoadingIcon 加载图标" :auto-back="true" />
    <view class="p-[16px]">
      <view class="demo-block">
        <text class="demo-label">基础用法</text>
        <UpLoadingIcon class="my-[10px]"></UpLoadingIcon>
      </view>

      <view class="demo-block">
        <text class="demo-label">其他模式</text>
        <UpLoadingIcon mode="circle" class="my-[10px]"></UpLoadingIcon>
        <UpLoadingIcon mode="semicircle" class="my-[10px]"></UpLoadingIcon>
      </view>
    </view>
  </view>
</template>

<script setup lang="uts">
import NavBar from '@/src/components/NavBar/NavBar.uvue';
import UpLoadingIcon from '@/uni_modules/uview-ultra/components/up-loading-icon/up-loading-icon.uvue';

definePage({
  style: {
    navigationStyle: 'custom',
    navigationBarTitleText: 'LoadingIcon 加载图标'
  }
});
</script>

<style lang="scss" scoped>
.demo-block {
  margin-bottom: 12px;
  padding: 12px;
  background-color: #ffffff;
  border-radius: 8px;
}
.demo-label {
  font-size: 14px;
  font-weight: bold;
  color: #606266;
  margin-bottom: 10px;
  padding-left: 4px;
  border-left-width: 3px;
  border-left-style: solid;
  border-left-color: $up-primary;
}
</style>
