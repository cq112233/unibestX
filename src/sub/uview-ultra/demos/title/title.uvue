<template>
  <view class="page-container bg-[#f8fafc] min-h-screen pb-[30px]">
    <NavBar title="up-title 标题"></NavBar>
    <view class="p-[16px]">
      <!-- 1. 基本使用 -->
      <view class="demo-block">
        <up-title class="demo-title">
          <text class="demo-title-text">基本使用（默认前缀竖条）</text>
        </up-title>
        <up-title>
          <text class="title-text">这里是标题</text>
        </up-title>
      </view>

      <!-- 2. 自定义前缀 -->
      <view class="demo-block mt-[12px]">
        <up-title class="demo-title">
          <text class="demo-title-text">自定义前缀（prefix 插槽）</text>
        </up-title>
        <up-title>
          <template #prefix>
            <view class="custom-prefix" :style="{ backgroundColor: themeColor }">
              <text class="custom-prefix-text">◆</text>
            </view>
          </template>
          <text class="title-text">带自定义前缀的标题</text>
        </up-title>
      </view>

      <!-- 3. 组合使用 -->
      <view class="demo-block mt-[12px]">
        <up-title class="demo-title">
          <text class="demo-title-text">组合使用（搭配 up-icon）</text>
        </up-title>
        <up-title>
          <up-icon name="bell" size="18" :color="themeColor"></up-icon>
          <text class="title-text">带图标的标题</text>
        </up-title>
      </view>

      <!-- 4. 多级标题样式 -->
      <view class="demo-block mt-[12px]">
        <up-title class="demo-title">
          <text class="demo-title-text">多级标题样式（通过 class 定制字号）</text>
        </up-title>
        <up-title>
          <text class="title-text title-lg">大号标题</text>
        </up-title>
        <view class="title-gap"></view>
        <up-title>
          <text class="title-text">默认标题</text>
        </up-title>
        <view class="title-gap"></view>
        <up-title>
          <text class="title-text title-sm">小号标题</text>
        </up-title>
      </view>

      <!-- 5. 说明 -->
      <view class="demo-block mt-[12px]">
        <up-title class="demo-title">
          <text class="demo-title-text">说明</text>
        </up-title>
        <text class="demo-text">
          up-title 已适配 vdom/vapor 双模式：无 props、无事件，仅通过默认插槽展示标题文本、通过 prefix 插槽自定义前缀；前缀竖条默认使用主题主色，可通过覆盖 .up-title 类定制整体样式，H5/小程序沿用原 .vue 实现。
        </text>
      </view>
    </view>
  </view>
</template>

<script setup lang="uts">
import { computed } from 'vue';
import { useAppStore } from '@/src/store/index.uts';

definePage({
  style: {
    navigationStyle: 'custom',
    navigationBarTitleText: 'up-title 标题'
  }
});

const appStore = useAppStore();
const themeColor = computed<string>(() => appStore.state.theme);
</script>

<style lang="scss" scoped>
.demo-block {
  margin-bottom: 12px;
  padding: 12px;
  background-color: #ffffff;
  border-radius: 8px;
}

.demo-title {
  margin-bottom: 10px;
}
.demo-title-text {
  font-size: 14px;
  font-weight: bold;
  color: #606266;
}

.demo-text {
  font-size: 13px;
  line-height: 20px;
  color: #64748b;
}

.title-text {
  font-size: 16px;
  font-weight: bold;
  color: #1e293b;
}

.title-lg {
  font-size: 22px;
}

.title-sm {
  font-size: 13px;
  font-weight: normal;
  color: #64748b;
}

.title-gap {
  height: 10px;
}

.custom-prefix {
  width: 18px;
  height: 18px;
  display: flex;
  flex-direction: row;
  align-items: center;
  justify-content: center;
  border-radius: 4px;
  margin-right: 8px;
}

.custom-prefix-text {
  font-size: 12px;
  line-height: 12px;
  color: #ffffff;
}
</style>
