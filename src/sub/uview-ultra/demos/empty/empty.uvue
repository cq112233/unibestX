<template>
  <view class="page-container bg-[#f8fafc] min-h-screen pb-[30px]">
    <NavBar title="Empty 空白页" :auto-back="true" />
    <view class="p-[16px]">
      <view class="demo-block">
        <text class="demo-label">基础用法</text>
        <UpEmpty mode="data" class="my-[20px]"></UpEmpty>
      </view>

      <view class="demo-block">
        <text class="demo-label">不同类型</text>
        <UpEmpty mode="car" class="my-[20px]"></UpEmpty>
        <UpEmpty mode="list" class="my-[20px]"></UpEmpty>
      </view>
    </view>
  </view>
</template>

<script setup lang="uts">
import NavBar from '@/src/components/NavBar/NavBar.uvue';
import UpEmpty from '@/uni_modules/uview-ultra/components/up-empty/up-empty.uvue';

definePage({
  style: {
    navigationStyle: 'custom',
    navigationBarTitleText: 'Empty 空白页'
  }
});
</script>

<style lang="scss" scoped>
.demo-block {
  margin-bottom: 12px;
  padding: 12px;
  background-color: #ffffff;
  border-radius: 8px;
}
.demo-label {
  font-size: 14px;
  font-weight: bold;
  color: #606266;
  margin-bottom: 10px;
  padding-left: 4px;
  border-left-width: 3px;
  border-left-style: solid;
  border-left-color: $up-primary;
}
</style>
