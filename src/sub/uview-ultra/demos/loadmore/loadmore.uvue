<template>
  <view class="page-container bg-[#f8fafc] min-h-screen pb-[30px]">
    <NavBar title="Loadmore 加载更多" :auto-back="true" />
    <view class="p-[16px]">
      <view class="demo-block">
        <text class="demo-label">基础用法</text>
        <UpLoadmore status="loadmore"></UpLoadmore>
      </view>
      <view class="demo-block mt-[12px]">
        <text class="demo-label">加载中</text>
        <UpLoadmore status="loading" loading-text="正在奋力加载中..."></UpLoadmore>
      </view>
      <view class="demo-block mt-[12px]">
        <text class="demo-label">没有更多了</text>
        <UpLoadmore status="nomore" nomore-text="我是有底线的"></UpLoadmore>
      </view>
      <view class="demo-block mt-[12px]">
        <text class="demo-label">显示线条</text>
        <UpLoadmore status="nomore" :line="true"></UpLoadmore>
      </view>
      <view class="demo-block mt-[12px]">
        <text class="demo-label">自定义图标和颜色</text>
        <UpLoadmore status="loading" loading-icon="spinner" color="#fa3534" icon-color="#fa3534"></UpLoadmore>
      </view>
    </view>
  </view>
</template>

<script setup lang="uts">
import NavBar from '@/src/components/NavBar/NavBar.uvue';
import UpLoadmore from '@/uni_modules/uview-ultra/components/up-loadmore/up-loadmore.uvue';

definePage({
  style: {
    navigationStyle: 'custom',
    navigationBarTitleText: 'Loadmore 加载更多'
  }
});
</script>

<style lang="scss" scoped>
.demo-block {
  margin-bottom: 12px;
  padding: 12px;
  background-color: #ffffff;
  border-radius: 8px;
}
.demo-label {
  font-size: 14px;
  font-weight: bold;
  color: #606266;
  margin-bottom: 10px;
  padding-left: 4px;
  border-left-width: 3px;
  border-left-style: solid;
  border-left-color: $up-primary;
}
</style>
