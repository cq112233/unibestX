<template>
  <view class="page-container bg-[#f8fafc] min-h-screen pb-[30px]">
    <NavBar title="Skeleton 骨架屏" :auto-back="true" />
    <view class="p-[16px]">
      <view class="demo-block">
        <text class="demo-label">基础用法</text>
        <UpSkeleton :loading="true" :animate="true" :rows="3" title></UpSkeleton>
      </view>

      <view class="demo-block">
        <text class="demo-label">带头像</text>
        <UpSkeleton :loading="true" :animate="true" :rows="2" title avatar></UpSkeleton>
      </view>
    </view>
  </view>
</template>

<script setup lang="uts">
import NavBar from '@/src/components/NavBar/NavBar.uvue';
import UpSkeleton from '@/uni_modules/uview-ultra/components/up-skeleton/up-skeleton.uvue';

definePage({
  style: {
    navigationStyle: 'custom',
    navigationBarTitleText: 'Skeleton 骨架屏'
  }
});
</script>

<style lang="scss" scoped>
.demo-block {
  margin-bottom: 12px;
  padding: 12px;
  background-color: #ffffff;
  border-radius: 8px;
}
.demo-label {
  font-size: 14px;
  font-weight: bold;
  color: #606266;
  margin-bottom: 10px;
  padding-left: 4px;
  border-left-width: 3px;
  border-left-style: solid;
  border-left-color: $up-primary;
}
</style>
