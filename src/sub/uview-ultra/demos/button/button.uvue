<template>
  <view class="page-container bg-[#f8fafc] min-h-screen pb-[30px]">
    <NavBar title="up-button 按钮" :auto-back="true"></NavBar>
    <view class="p-[16px]">
      <view class="demo-block">
        <text class="demo-label">基础用法</text>
        <view class="mb-[10px]">
          <UpButton type="primary" text="主要按钮"></UpButton>
        </view>
        <view class="mb-[10px]">
          <UpButton type="success" text="成功按钮"></UpButton>
        </view>
        <view class="mb-[10px]">
          <UpButton type="info" text="信息按钮"></UpButton>
        </view>
        <view class="mb-[10px]">
          <UpButton type="warning" text="警告按钮"></UpButton>
        </view>
        <view class="mb-[10px]">
          <UpButton type="error" text="危险按钮"></UpButton>
        </view>
      </view>

      <view class="demo-block">
        <text class="demo-label">镂空按钮</text>
        <view class="mb-[10px]">
          <UpButton type="primary" text="主要按钮" :plain="true"></UpButton>
        </view>
      </view>

      <view class="demo-block">
        <text class="demo-label">按钮尺寸</text>
        <view class="flex-row items-center mb-[10px]">
          <UpButton type="primary" text="大尺寸" size="large"></UpButton>
        </view>
        <view class="flex-row items-center mb-[10px]">
          <UpButton type="primary" text="普通尺寸" size="normal"></UpButton>
        </view>
        <view class="flex-row items-center mb-[10px]">
          <UpButton type="primary" text="小尺寸" size="small"></UpButton>
        </view>
        <view class="flex-row items-center mb-[10px]">
          <UpButton type="primary" text="迷你尺寸" size="mini"></UpButton>
        </view>
      </view>
    </view>
  </view>
</template>

<script setup lang="uts">
import NavBar from '@/src/components/NavBar/NavBar.uvue';
import UpButton from '@/uni_modules/uview-ultra/components/up-button/up-button.uvue';

definePage({
  style: {
    navigationStyle: 'custom',
    navigationBarTitleText: 'up-button 按钮'
  }
});
</script>

<style lang="scss" scoped>
.demo-block {
  margin-bottom: 12px;
  padding: 12px;
  background-color: #ffffff;
  border-radius: 8px;
}
.demo-label {
  font-size: 14px;
  font-weight: bold;
  color: #606266;
  margin-bottom: 10px;
  padding-left: 4px;
  border-left-width: 3px;
  border-left-style: solid;
  border-left-color: $up-primary;
}
</style>
