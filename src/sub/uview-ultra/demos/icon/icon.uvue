<template>
  <view class="page-container bg-[#f8fafc] min-h-screen pb-[30px]">
    <NavBar title="Icon 图标" :auto-back="true" />
    <view class="p-[16px]">
      <view class="demo-block">
        <text class="demo-label">基础用法</text>
        <view class="flex-row flex-wrap">
          <UpIcon name="photo" size="28" class="mr-[15px] mb-[15px]"></UpIcon>
          <UpIcon name="lock" size="28" class="mr-[15px] mb-[15px]"></UpIcon>
          <UpIcon name="star" size="28" class="mr-[15px] mb-[15px]"></UpIcon>
        </view>
      </view>

      <view class="demo-block">
        <text class="demo-label">颜色定制</text>
        <view class="flex-row flex-wrap">
          <UpIcon name="heart-fill" color="#fa3534" size="28" class="mr-[15px] mb-[15px]"></UpIcon>
          <UpIcon name="github-circle-fill" color="#19be6b" size="28" class="mr-[15px] mb-[15px]"></UpIcon>
          <UpIcon name="google-circle-fill" color="#2979ff" size="28" class="mr-[15px] mb-[15px]"></UpIcon>
        </view>
      </view>
    </view>
  </view>
</template>

<script setup lang="uts">
import NavBar from '@/src/components/NavBar/NavBar.uvue';
import UpIcon from '@/uni_modules/uview-ultra/components/up-icon/up-icon.uvue';

definePage({
  style: {
    navigationStyle: 'custom',
    navigationBarTitleText: 'Icon 图标'
  }
});
</script>

<style lang="scss" scoped>
.demo-block {
  margin-bottom: 12px;
  padding: 12px;
  background-color: #ffffff;
  border-radius: 8px;
}
.demo-label {
  font-size: 14px;
  font-weight: bold;
  color: #606266;
  margin-bottom: 10px;
  padding-left: 4px;
  border-left-width: 3px;
  border-left-style: solid;
  border-left-color: $up-primary;
}
</style>
