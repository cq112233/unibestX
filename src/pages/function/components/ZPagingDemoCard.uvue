<template>
  <Card title="数据分页列表 (z-paging-x & NestedScroll)">
    <!-- 顶部描述与特性标签 -->
    <view class="flex-col mb-[12px]">
      <text class="text-[13px] text-[#64748b] leading-[18px] mb-[10px]">
        集成了下拉刷新、触底平滑加载、异常重试与空状态兜底等完整能力，适配移动端原生顶级高刷性能。
      </text>

      <!-- 特性矩阵徽章 -->
      <view class="flex flex-row flex-wrap justify-between mb-[12px]">
        <view class="w-[48.5%] p-[8px] mb-[6px] rounded-[8px] bg-[#0957de0f] border-[1px] border-solid border-[#0957de26] flex flex-row items-center">
          <text class="text-[14px] mr-[6px]">🚀</text>
          <text class="text-[11px] font-bold text-[#0957de]">120Hz 极速分页</text>
        </view>
        <view class="w-[48.5%] p-[8px] mb-[6px] rounded-[8px] bg-[#10b9810f] border-[1px] border-solid border-[#10b98126] flex flex-row items-center">
          <text class="text-[14px] mr-[6px]">🔄</text>
          <text class="text-[11px] font-bold text-[#059669]">原生阻尼下拉</text>
        </view>
        <view class="w-[48.5%] p-[8px] rounded-[8px] bg-[#f59e0b0f] border-[1px] border-solid border-[#f59e0b26] flex flex-row items-center">
          <text class="text-[14px] mr-[6px]">⚡</text>
          <text class="text-[11px] font-bold text-[#d97706]">触底平滑追加</text>
        </view>
        <view class="w-[48.5%] p-[8px] rounded-[8px] bg-[#8b5cf60f] border-[1px] border-solid border-[#8b5cf626] flex flex-row items-center">
          <text class="text-[14px] mr-[6px]">🛡️</text>
          <text class="text-[11px] font-bold text-[#7c3aed]">空态/异常兜底</text>
        </view>
      </view>
    </view>

    <!-- 静态数据样例预览（自适应高度，无局部滚动条，彻底避免嵌套手势死锁） -->
    <view class="rounded-[12px] p-[12px] bg-[#f8fafc] border-[1px] border-solid border-[#e2e8f0] mb-[14px]">
      <view class="flex flex-row items-center justify-between mb-[8px]">
        <text class="text-[12px] font-bold text-[#1e293b]">📋 数据项预览</text>
        <text class="text-[10px] text-[#94a3b8]">独立页面完美分页</text>
      </view>

      <view
        v-for="item in previewItems"
        :key="item.id"
        class="p-[10px] mb-[8px] bg-white rounded-[8px] border-[1px] border-solid border-[#edf2f7]"
      >
        <view class="flex-row justify-between items-center mb-[4px]">
          <view
            class="px-[6px] py-[1px] rounded-[4px]"
            :style="{ backgroundColor: item.tagBgColor }"
          >
            <text class="text-[9px] font-bold" :style="{ color: item.tagColor }">{{ item.tag }}</text>
          </view>
          <text class="text-[10px] text-[#94a3b8]">#{{ item.id }}</text>
        </view>
        <text class="text-[13px] font-bold text-[#1e293b] mb-[2px]">{{ item.title }}</text>
        <text class="text-[11px] text-[#64748b] leading-[15px]">{{ item.summary }}</text>
      </view>
    </view>

    <!-- 双按钮导航：分别提供 z-paging-x 与自研 NestedScroll 独立演示 -->
    <view class="flex flex-col">
      <view
        class="w-full h-[42px] rounded-[10px] bg-[#0957de] flex flex-row items-center justify-center shadow-sm mb-[8px]"
        @click="navigateToZPaging"
      >
        <text class="text-[13px] text-white font-bold mr-[6px]">🚀 进入 z-paging-x 原生分页独立演示</text>
        <text class="text-[13px] text-white font-bold">→</text>
      </view>

      <view
        class="w-full h-[42px] rounded-[10px] bg-[#4f46e5] flex flex-row items-center justify-center shadow-sm"
        @click="navigateToNestedScroll"
      >
        <text class="text-[13px] text-white font-bold mr-[6px]">⚡ 进入 NestedScroll 自研嵌套滚动演示</text>
        <text class="text-[13px] text-white font-bold">→</text>
      </view>
    </view>
  </Card>
</template>

<script setup lang="uts">
import Card from '../../basic/components/Card.uvue';

type PreviewItem = {
  id: number;
  title: string;
  summary: string;
  tag: string;
  tagBgColor: string;
  tagColor: string;
};

const previewItems: PreviewItem[] = [
  {
    id: 1,
    title: '深入解析 uni-app X 跨端高性能分页实践',
    summary: '基于 UTS 强类型系统与原生渲染管道构建的高性能架构。',
    tag: 'z-paging-x',
    tagBgColor: '#0957de14',
    tagColor: '#0957de'
  },
  {
    id: 2,
    title: '嵌套滚动 nested-scroll 与触底加载设计规范',
    summary: '自研组件原生手势联通与平滑衔接，彻底消除双重滚动冲突。',
    tag: 'NestedScroll',
    tagBgColor: '#6366f114',
    tagColor: '#4f46e5'
  }
];

function navigateToZPaging(): void {
  uni.navigateTo({
    url: '/src/sub/zpaging/zpaging'
  });
}

function navigateToNestedScroll(): void {
  uni.navigateTo({
    url: '/src/sub/nested-scroll/nested-scroll'
  });
}
</script>

<style scoped></style>
