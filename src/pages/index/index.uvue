<template>
  <view>
    <view class="content-container">
      <image class="w-[100px] h-[100px] mx-auto mt-[20px] rounded-[20px]" src="/static/logo.png" />
      <text class="text-[26px] font-semibold mx-auto mt-[10px] text-[#d14328]">
        unibestX
      </text>
      <text class="text-center text-[18px] text-[#334155] font-medium mt-[6px]">
        最好用的 uni-appX 开发模板
      </text>
      <!-- 编译运行环境状态卡片 -->
      <view class="mx-[16px] mt-[16px] px-[6px] py-[10px] rounded-[14px] border-[1px] border-solid border-[#e2e8f0] flex flex-row items-center justify-between">
        <!-- 运行平台 -->
        <view class="flex-1 items-center justify-center">
          <text class="text-[10px] text-[#94a3b8] mb-[2px]">编译平台</text>
          <text class="text-[12px] font-bold text-[#4f46e5]">{{ systemInfo?.platformName ?? 'H5' }}</text>
        </view>
        <view class="w-[1px] h-[20px] bg-[#e2e8f0]" />
        <!-- 当前环境 -->
        <view class="flex-1 items-center justify-center">
          <text class="text-[10px] text-[#94a3b8] mb-[2px]">当前环境</text>
          <text
            class="text-[12px] font-bold"
            :style="{ color: (systemInfo?.env == 'test' ? '#d97706' : (systemInfo?.env == 'production' ? '#dc2626' : '#059669')) }"
          >
            {{ systemInfo?.env == 'test' ? '测试' : (systemInfo?.env == 'production' ? '生产' : '开发') }}
          </text>
        </view>
        <view class="w-[1px] h-[20px] bg-[#e2e8f0]" />
        <!-- 渲染模式 -->
        <view class="flex-1 items-center justify-center">
          <text class="text-[10px] text-[#94a3b8] mb-[2px]">渲染模式</text>
          <text class="text-[12px] font-bold text-[#059669]">{{ systemInfo?.isVapor == true ? 'Vapor' : 'VDOM' }}</text>
        </view>
        <view class="w-[1px] h-[20px] bg-[#e2e8f0]" />
        <!-- 应用版本 -->
        <view class="flex-1 items-center justify-center">
          <text class="text-[10px] text-[#94a3b8] mb-[2px]">应用版本</text>
          <text class="text-[12px] font-bold text-[#d97706]">v{{ systemInfo?.appVersion ?? '1.0.0' }}</text>
        </view>
        <view class="w-[1px] h-[20px] bg-[#e2e8f0]" />
        <!-- 编译引擎版本 -->
        <view class="flex-1 items-center justify-center">
          <text class="text-[10px] text-[#94a3b8] mb-[2px]">编译引擎</text>
          <text class="text-[12px] font-bold text-[#334155]">{{ (systemInfo?.compilerVersion ?? '') != '' ? systemInfo?.compilerVersion : '1.0.0' }}</text>
        </view>
      </view>
      <view>
        <text class="mt-[10px] px-[30px] text-[14px] text-[#475569] leading-[26px]">
          　　unibestX 是一个集成了多种工具与技术的 uni-appX 开发模板，由 uni-appX + Vue3 + Ts + weapp-tailwindcss + VSCode 构建，模板具有代码提示、自动格式化、统一配置、代码片段等功能，并内置了 ECharts 图表、主题配置、暗黑模式、加密方式配置等常用功能与基本组件，让你编写 uni-appX 拥有 best 体验。
        </text>
      </view>
      <view class="mt-[15px] mx-[30px] px-[20px] py-[15px] rounded-[12px] bg-[#37c2bc14]">
        <text class="text-[14px] font-bold text-[#0f8f8a]">
          ⚡ 渲染模式：本项目全面支持 VDOM / Vapor 两种模式，默认 Vapor 蒸汽模式
        </text>
        <text class="text-[14px] text-[#666666] leading-[26px]">
          　　Vapor 蒸汽模式是新一代渲染引擎，渲染性能超过原生，Android 端语法要求更宽松，AI 友好度与动态性更强；从 2026 年起将逐渐替代老的 VDOM 模式。
        </text>
        <text class="text-[14px] text-[#666666] leading-[26px]">
          　　推荐使用 Vapor 蒸汽模式，可在 manifest.json 中按需切换回 VDOM 模式。
        </text>
        <text class="text-[14px] text-[#666666] leading-[26px]">
          　　注意：Vapor 模式下开发过的代码，切换回 VDOM 模式可能因编译检查更严格而报类型或语法错误，请按需选择。
        </text>
      </view>
      <view class="mt-[15px] mx-[30px] px-[20px] py-[15px] rounded-[12px] bg-[#8b5cf614]">
        <text class="text-[14px] font-bold text-[#7c3aed]">
          🧪 沙盒独立调试模式（Page Sandbox）：单页/模块秒级极速开发
        </text>
        <text class="text-[14px] text-[#666666] leading-[26px]">
          　　大型项目或分包页面众多时，开启沙盒模式可仅编译指定目标页面（自动置为启动首页），跳过全量页面构建与 TabBar 启动开销，启动与热重载快如闪电。
        </text>
        <text class="text-[14px] text-[#475569] font-medium leading-[26px]">
          　　• 方式一（代码级・推荐）：在任意页面的 definePage 中配置：
        </text>
        <text class="text-[13px] text-[#64748b] leading-[24px] pl-[10px]">
          　　  - debug: true（开启当前页面沙盒调试）
        </text>
        <text class="text-[13px] text-[#64748b] leading-[24px] pl-[10px]">
          　　  - debugHome: true（将当前页面设为沙盒默认启动首页，排在第 1 位）
        </text>
        <text class="text-[13px] text-[#64748b] leading-[24px] pl-[10px]">
          　　  - debug: false（拥有最高一票否决权，调试完成后改回 false 即可退出沙盒）
        </text>
        <text class="text-[14px] text-[#475569] font-medium leading-[26px] mt-[4px]">
          　　• 方式二（环境级・批量）：在 .env 文件中配置：
        </text>
        <text class="text-[13px] text-[#64748b] leading-[24px] pl-[10px]">
          　　  - VITE_DEV_SANDBOX=true（开启环境级沙盒）
        </text>
        <text class="text-[13px] text-[#64748b] leading-[24px] pl-[10px]">
          　　  - VITE_DEV_SANDBOX_PAGES=src/sub/auth/login, src/sub/auth/*（指定单页/多页/通配符路径）
        </text>
        <text class="text-[13px] text-[#d97706] font-medium leading-[24px] pl-[10px]">
          　　  ⭐ 优先级规则：.env 开启时拥有最高优先级，完全以 .env 指定的页面为主（自动覆盖代码内的 debug 声明）；.env 关闭时才回退按代码级 debug 独立调试。
        </text>
        <text class="text-[14px] text-[#7c3aed] font-medium leading-[26px] mt-[6px]">
          　　🔒 安全保护：调试期间全内存路由接管，绝不修改与污染 pages.config.json 生产全量配置！
        </text>
      </view>
      <view class="mt-[15px] mx-[30px] px-[20px] py-[15px] rounded-[12px] bg-[#3182ce14]">
        <text class="text-[14px] font-bold text-[#3182ce]">
          🎨 样式引擎：Tailwind CSS v4 + weapp-tailwindcss
        </text>
        <text class="text-[14px] text-[#666666] leading-[26px]">
          　　weapp-tailwindcss 是 uni-app X 的 Tailwind CSS 适配方案，类名写法与 Web 端完全一致：支持方括号任意值（text-[14px]、mt-[12px]、bg-[#3b82f6] 等），免去手写 CSS。
        </text>
        <text class="text-[14px] text-[#666666] leading-[26px]">
          　　内置 dark: 暗黑变体与 wx:、h5:、app: 等平台变体，一套类名多端生效；支持 @apply 样式复用，可在「基础 → weapp-tailwindcss Demo」查看完整示例。
        </text>
      </view>
      <view class="mt-[15px] mx-[30px] px-[20px] py-[15px] rounded-[12px] bg-[#6366f114]">
        <text class="text-[14px] font-bold text-[#4f46e5]">
          📦 UI 组件库说明（当前分支未内置组件库）
        </text>
        <text class="text-[14px] text-[#666666] leading-[26px]">
          　　当前 main 分支为轻量纯净底座，未自带第三方 UI 组件库，方便开发者按需接入或使用 Tailwind CSS 灵活封装。
        </text>
        <text class="text-[14px] text-[#666666] leading-[26px]">
          　　如需使用开箱即用的成套组件库，可切换至对应组件库分支：
        </text>
        <text class="text-[14px] text-[#4f46e5] font-medium leading-[26px]">
          　　• uniX-rice-ui 分支：集成 Rice UI 组件库（推荐，官方团队持续维护）
        </text>
        <text class="text-[14px] text-[#4f46e5] font-medium leading-[26px]">
          　　• uniX-uview-ultra 分支：集成 uview-ultra 组件库
        </text>
      </view>
      <view class="mt-[15px] mx-[30px] px-[20px] py-[15px] rounded-[12px] bg-[#f59e0b1a]">
        <text class="text-[14px] font-bold text-[#d97706]">
          ⚠️ 页面路由配置须知（重要）
        </text>
        <text class="text-[14px] text-[#666666] leading-[26px]">
          　　本项目内置 vite-plugin-uni-pages 自动文件路由插件：当页面代码中有 definePage 或 &lt;route&gt; 配置时，插件会自动双向同步 pages.config.json 和 pages.json；当页面中未写配置时，请在 pages.config.json 中配置，也会自动同步到 pages.json。
        </text>
        <text class="text-[14px] font-bold mt-[4px] text-[#d97706] leading-[24px]">
          　　注意：pages.json 为自动构建产物，切勿直接在 pages.json 中修改！
        </text>
      </view>
      <view class="mt-[15px] mx-[30px] px-[20px] py-[15px] rounded-[12px] bg-[#37c2bc14] mb-[30px]">
        <text class="text-[14px] text-[#666666] leading-[26px]">
          📄 文件路由 — 自动扫描路由，通过 pages.config.json / definePage 统一配置
        </text>
        <text class="text-[14px] text-[#666666] leading-[26px]">
          ⚡ 渲染模式 — 全面支持 VDOM / Vapor 两种模式，默认 Vapor 蒸汽模式
        </text>
        <text class="text-[14px] text-[#666666] leading-[26px]">
          🧱 样式引擎 — Tailwind CSS v4 + weapp-tailwindcss，方括号任意值与 dark: 变体全端生效
        </text>
        <text class="text-[14px] text-[#666666] leading-[26px]">
          📦 组件库选型 — 当前为轻量基础版（未自带组件库），提供 uniX-rice-ui / uniX-uview-ultra 专属分支
        </text>
        <text class="text-[14px] text-[#666666] leading-[26px]">
          📱 系统信息 — 获取安全区域、屏幕尺寸、设备像素比等系统参数
        </text>
        <text class="text-[14px] text-[#666666] leading-[26px]">
          🎨 图标字体 — 支持 uni-icon、lime-icon 双图标方案
        </text>
        <text class="text-[14px] text-[#666666] leading-[26px]">
          🌐 多语言切换 — 内置 i18n 国际化方案，中英文一键切换
        </text>
        <text class="text-[14px] text-[#666666] leading-[26px]">
          🎭 主题配置 — 五种预设主题色 + 暗黑模式（auto / light / dark），原生与组件同步生效
        </text>
        <text class="text-[14px] text-[#666666] leading-[26px]">
          🕐 日期时间 — 基于 lime-dayuts，实时时钟、日期偏移、月份边界等
        </text>
        <text class="text-[14px] text-[#666666] leading-[26px]">
          🔀 路由拦截 — 登录鉴权路由守卫，未登录自动跳转登录页
        </text>
        <text class="text-[14px] text-[#666666] leading-[26px]">
          🌐 网络请求 — 封装 lime-request，Mock 数据与真实接口演示
        </text>
        <text class="text-[14px] text-[#666666] leading-[26px]">
          🔐 加密配置 — 内置 unix-crypto，AES / DES / MD5 / SHA-256 / HMAC-SHA1 / Base64 / RSA 全端加密解密
        </text>
        <text class="text-[14px] text-[#666666] leading-[26px]">
          📊 图表渲染 — ECharts 图表集成，折线图、柱状图、饼图自适应
        </text>
        <text class="text-[14px] text-[#666666] leading-[26px]">
          📋 分页列表 — z-paging-x 分页加载，Tab 筛选、错误模拟、空数据
        </text>
      </view>
    </view>
  </view>
</template>

<script setup lang="uts">
import { handleBackPressExit } from '../../utils/backPress.uts';
import { onNavbarPullDownRefresh, stopNavbarPullDownRefresh } from '../../utils/refresh.uts';
import { systemInfo } from '../../utils/systemInfo.uts';

definePage({
  debug: false, // 是否开启当前页面沙盒独立调试（设为 true 可单独调试此页；⚠️ 调试完成后改回 false）
  debugHome: false, // 沙盒调试模式下是否将当前页面指定为启动首页（全局仅允许 1 个页面设为 true，排在 pages[0]）
  type: 'home', // 页面类型标识（仅首页配置 'home'，标记当前页面为应用全局启动首页，自动排在 pages[0]）
  layout: 'navbar', // 页面通用布局模板（采用 navbar 布局接管顶部导航与下拉滚动容器）
  showBack: false, // 导航栏左侧是否显示返回箭头（TabBar 首页无需返回按钮）
  hideNavbar: false, // 是否隐藏顶部导航栏（false 为正常显示）
  enablePullDownRefresh: true, // 是否开启自定义下拉刷新（由 navbar 布局内的 scroll-view 驱动）
  style: {
    navigationBarTitleText: '首页', // 页面与导航栏标题文本
    navigationStyle: 'custom' // 导航栏样式设为自定义（隐藏系统原生导航栏）
  }
});

// 监听 navbar 自定义下拉刷新事件（在 enablePullDownRefresh: true 时生效）
onNavbarPullDownRefresh(() => {
  setTimeout(() => {
    // 数据加载完毕后，手动停止下拉刷新动画状态
    stopNavbarPullDownRefresh();
  }, 1000);
});

onBackPress((options: OnBackPressOptions): boolean => {
  // #ifdef APP
  if (options.from == 'backbutton') {
    return handleBackPressExit();
  }
  // #endif
  return false;
});
</script>

<style lang="scss" scoped>
</style>
