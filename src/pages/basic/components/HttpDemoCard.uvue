<template>
  <Card title="HTTP 请求 Demo">
    <view class="flex-col mb-[14px]">
      <text
        class="text-[13px] leading-[18px]"
        :style="{ color: 'var(--text-secondary, #64748b)' }"
      >
        基于 lime-request 封装的 http 请求演示，包含 Mock 数据、真实接口请求、uniCloud 云对象与 OSS 文件上传四种模式。内容较多，已拆分到独立页面。
      </text>
    </view>

    <!-- 能力清单 -->
    <view class="flex-row flex-wrap mb-[14px]">
      <view class="rounded-[6px] px-[8px] py-[4px] bg-[#eff6ff] mr-[8px] mb-[8px]">
        <text class="text-[11px] text-[#1d4ed8]">Mock 数据</text>
      </view>
      <view class="rounded-[6px] px-[8px] py-[4px] bg-[#ecfdf5] mr-[8px] mb-[8px]">
        <text class="text-[11px] text-[#065f46]">真实 API 请求</text>
      </view>
      <view class="rounded-[6px] px-[8px] py-[4px] bg-[#f0fdfa] mr-[8px] mb-[8px]">
        <text class="text-[11px] text-[#0f766e]">uniCloud 云对象</text>
      </view>
      <view class="rounded-[6px] px-[8px] py-[4px] bg-[#fff7ed] mr-[8px] mb-[8px]">
        <text class="text-[11px] text-[#c2410c]">OSS 文件上传</text>
      </view>
    </view>

    <!-- 进入独立页面按钮 -->
    <view
      class="w-full h-[44px] rounded-[8px] flex flex-row items-center justify-center"
      :style="{ backgroundColor: 'var(--theme-color, #0f766e)' }"
      @click="navigateToDemo"
    >
      <text class="text-[#ffffff] text-[14px] font-bold">进入 HTTP 请求 Demo 页</text>
    </view>
  </Card>
</template>

<script setup lang="uts">
import Card from './Card.uvue';

function navigateToDemo() {
  uni.navigateTo({
    url: '/src/sub/httpDemo/httpDemo',
    fail: (err) => {
      console.error('navigateToDemo fail:', err);
    }
  });
}
</script>

<style scoped></style>
