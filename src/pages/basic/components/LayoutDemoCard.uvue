<template>
  <Card title="布局页面示例">
    <view class="flex-col mb-[16px]">
      <text class="text-[13px] text-[#64748b] leading-[18px]">
        演示基于 definePage + navbar 布局模板的页面骨架。点击进入后可在示例页中自行编写内容，测试
        navbar 顶部导航栏、showBack 返回、hideNavbar、下拉刷新等布局表现。
      </text>
    </view>

    <!-- 进入示例页面按钮 -->
    <view
      class="w-full h-[44px] rounded-[8px] bg-[#3b82f6] flex flex-row items-center justify-center"
      @click="navigateToDemo"
    >
      <text class="text-[#ffffff] text-[14px] font-bold">进入布局页面示例</text>
    </view>
  </Card>
</template>

<script setup lang="uts">
import Card from './Card.uvue';

function navigateToDemo() {
  uni.navigateTo({
    url: '/src/sub/layoutDemo/layoutDemo',
    fail: (err) => {
      console.error('navigateToDemo fail:', err);
    }
  });
}
</script>

<style>
</style>
