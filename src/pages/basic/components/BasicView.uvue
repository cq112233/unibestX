<template>
  <view class="flex-col">
    <view class="flex-col">
      <!-- 布局页面示例组件 -->
      <LayoutDemoCard />

      <!-- weapp-tailwindcss 样式引擎示例 -->
      <TailwindcssDemoCard />

      <!-- 使用封装的主题切换组件 -->
      <ThemeSwitchCard />

      <!-- 使用路由拦截与传参演示组件 -->
      <RouterDemoCard />

      <!-- 使用封装的多语言切换组件 -->
      <LangSwitchCard />

      <!-- 使用封装的图标示例组件 -->
      <IconDemoCard />

      <!-- HTTP 请求 Demo 组件 -->
      <HttpDemoCard />

      <!-- 全局 Toast 轻提示演示组件 -->
      <ToastDemoCard />

      <!-- 父子组件传参 & 具名插槽演示组件 -->
      <PropsDemoCard />

      <!-- 系统与安全区域信息组件 -->
      <SystemInfoDemoCard />
    </view>
  </view>
</template>

<script setup lang="uts">
import ToastDemoCard from './ToastDemoCard.uvue';
import PropsDemoCard from './PropsDemoCard.uvue';
import IconDemoCard from './IconDemoCard.uvue';
import LangSwitchCard from './LangSwitchCard.uvue';
import LayoutDemoCard from './LayoutDemoCard.uvue';
import ThemeSwitchCard from './ThemeSwitchCard.uvue';
import RouterDemoCard from './RouterDemoCard.uvue';
import HttpDemoCard from './HttpDemoCard.uvue';
import TailwindcssDemoCard from './TailwindcssDemoCard.uvue';
import SystemInfoDemoCard from './SystemInfoDemoCard.uvue';
</script>

<style scoped></style>
