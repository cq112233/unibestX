<template>
  <BasicView />
</template>

<script setup lang="uts">
import BasicView from './views/BasicView.uvue';
import { handleBackPressExit } from '@/src/utils/backPress/index.uts';
import { onNavbarPullDownRefresh, stopNavbarPullDownRefresh } from '@/src/utils/refresh/index.uts';
import { notifyPageHide, notifyPageShow, setCurIdx } from '@/src/tabbar/index.uts';

definePage({
  debug: false,
  layout: 'navbar',
  showBack: false,
  enablePullDownRefresh: true,
  hideStatusBar: false,
  hideNavbar: false,
  style: {
    navigationBarTitleText: '基础',
    navigationStyle: 'custom'
  }
});

// 独立页面模式下，切到当前基础页时同步当前 Tab 激活索引并通知页面可见
onShow(() => {
  setCurIdx(1);
  notifyPageShow();
});

onHide(() => {
  notifyPageHide();
});

onNavbarPullDownRefresh(() => {
  setTimeout(() => {
    stopNavbarPullDownRefresh();
  }, 1000);
});

onBackPress((options: OnBackPressOptions): boolean => {
  // #ifdef APP
  if (options.from == 'backbutton') {
    return handleBackPressExit();
  }
  // #endif
  return false;
});
</script>

<style></style>
