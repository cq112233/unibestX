import { DrawContext as DrawContext } from "@ohos.arkui.node";
import { RenderNode as RenderNode } from "@ohos.arkui.node";
import { DrawableContext as IDrawableContext } from '@dcloudio/uni-app-x/types/native';
export declare class DrawableContext extends RenderNode implements IDrawableContext {
    private _font;
    private _fontSize;
    private _fillStyle;
    private _strokeStyle;
    private _textAlign;
    private _lineJoin;
    private _lineCap;
    private _lineWidth;
    private _lineWidthToPixel;
    private _lineDashOffset;
    private _lineDashOffsetToPixel;
    private _drawFlag;
    private _drawingFont;
    private _drawingPen;
    private _drawingBrush;
    private _currentPath;
    private _commandQueue;
    private _firstFlag;
    private _firstMoveTo;
    constructor();
    draw(context: DrawContext): void;
    beginPath(): void;
    closePath(): void;
    arc(x: number, y: number, radius: number, startAngle: number, endAngle: number, anticlockwise?: boolean | null): void;
    bezierCurveTo(cp1x: number, cp1y: number, cp2x: number, cp2y: number, x: number, y: number): void;
    moveTo(x: number, y: number): void;
    lineTo(x: number, y: number): void;
    stroke(): void;
    strokeRect(x: number, y: number, width: number, height: number): void;
    strokeText(text: string, x: number, y: number): void;
    fill(fillRule?: string | null): void;
    fillRect(x: number, y: number, width: number, height: number): void;
    fillText(text: string, x: number, y: number): void;
    rect(x: number, y: number, width: number, height: number): void;
    setLineDash(segments: number[]): void;
    reset(): void;
    update(): void;
    get font(): string;
    set font(value: string);
    get fillStyle(): string;
    set fillStyle(color: string);
    get lineCap(): string;
    set lineCap(value: string);
    get lineDashOffset(): number;
    set lineDashOffset(value: number);
    get lineJoin(): string;
    set lineJoin(value: string);
    get lineWidth(): number;
    set lineWidth(value: number);
    get strokeStyle(): string;
    set strokeStyle(color: string);
    get textAlign(): string;
    set textAlign(value: string);
    private get currentPath();
    private textAlignOffset;
    private convertToRect;
    private setPathFillType;
}
