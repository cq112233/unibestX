import { UniElement as IUniElement, UniSwiperElement as IUniSwiperElement } from '@dcloudio/uni-app-x/types/native';
import { UniElementImpl } from './UniElement';
import { typeNode as typeNode } from "@ohos.arkui.node";
import { NativeMap as Map } from './NativeMap';
import { UniTouchEvent } from './UniTouchEvent';
export declare class UniSwiperElementImpl extends UniElementImpl<typeNode.Swiper> implements IUniSwiperElement {
    private controller;
    private showIndicator;
    private indicatorStyle;
    private duration;
    private touching;
    private touchEndTimer;
    private preventTouchMove;
    private vertical;
    private interval;
    private autoPlay;
    private loop;
    private displayMultipleItems;
    private currentIndex;
    private isAppear;
    protected lastTouchEvent: UniTouchEvent | null;
    private needPreventTouchMoveNodes;
    private styleCache;
    private needDisableSwipe;
    protected maxChildHeight: number;
    protected hasOwnHeight: boolean;
    protected hasInitHeight: boolean;
    protected hasOwnFlexGrow: boolean;
    private isUpdatingAutoHeight;
    static get defaultStyle(): Map<string, [string] | [string, boolean]>;
    protected initClass(): void;
    constructor();
    protected createFrameNode(): typeNode.Swiper;
    parseStyle(key: string, value: Object): boolean;
    insertBefore(child: IUniElement, anchor?: IUniElement | null | undefined): IUniElement | null;
    removeChild(aChild: IUniElement): IUniElement | null;
    removeStyleValue(property: string): void;
    updateNativeAttribute(key: string, value: ESObject): void;
    protected handleIndexChange(): void;
    protected getSpecifiedIndexSwiperItemId(index: number): string;
    protected handleAutoPlayNotLoopBehavior(index: number): void;
    protected initNativeEvent(type: string): void | Object;
    protected deleteNativeEvent(type: string, res: void | Object): boolean;
    dispose(): void;
    checkLayout(): void;
    private isViewportSwiper;
    private updateSwiperItemViewportLayout;
    private updateSwiperItemsViewportLayout;
    private updateAutoHeightByChildren;
    getHarmonyController<T extends SwiperController>(): T;
}
