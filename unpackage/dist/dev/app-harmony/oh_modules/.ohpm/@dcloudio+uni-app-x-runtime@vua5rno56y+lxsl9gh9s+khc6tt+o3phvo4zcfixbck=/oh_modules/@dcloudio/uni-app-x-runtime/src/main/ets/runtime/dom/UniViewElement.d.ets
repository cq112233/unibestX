import { UniViewElement as IUniViewElement, RequestFullscreenOptions, ExitFullscreenFailCallback, ExitFullscreenOptions } from '@dcloudio/uni-app-x/types/native';
import { UniHoverElementImpl } from './UniHoverElement';
import { FrameNode as FrameNode } from "@ohos.arkui.node";
import { DrawableContext } from './DrawableContext';
import { FullscreenError, fullscreenchangeEvent } from './UniElementRequestFullscreen';
export type RequestFullscreenOrientation = 'auto' | 'landscape' | 'landscape-primary' | 'landscape-secondary' | 'portrait';
type fullscreenchangeCallback = (event: fullscreenchangeEvent) => void;
export declare class UniViewElementImpl<T extends FrameNode | null = FrameNode | null> extends UniHoverElementImpl<T> implements IUniViewElement {
    static skipDefaultFrameNodeStyle: boolean;
    private _drawableContext;
    private fullscreenchangeCallbacks;
    private fullscreenerrorCallbacks;
    protected requestFullscreenOptions: RequestFullscreenOptions;
    constructor();
    protected createFrameNode(): T;
    checkLayout(): void;
    getDrawableContext(): DrawableContext;
    private _updateDrawableContextSizeIfNeed;
    protected setNavigation(status: 'show' | 'hide'): void;
    protected resolveIsFullscreenFrameNode(options?: RequestFullscreenOptions | null): Promise<void>;
    private resolveFullscreenFrameNode;
    private resetLayoutSize;
    private fullscreenFrameNodeOnWindowSizeChange;
    private setFullscreenFrameNodeStyle;
    protected requestFullscreenError(): void;
    requestFullscreen(options: RequestFullscreenOptions | null): void;
    fullscreenchange(callback: fullscreenchangeCallback): void;
    protected callFullscreenchangeCallbacks(): void;
    fullscreenerror(callback: ExitFullscreenFailCallback): void;
    protected callFullscreenerrorCallbacks(error?: FullscreenError | string): void;
    protected exitFullscreenResetStyle(): void;
    _exitFullscreen(options?: ExitFullscreenOptions | null): boolean | void;
}
export {};
