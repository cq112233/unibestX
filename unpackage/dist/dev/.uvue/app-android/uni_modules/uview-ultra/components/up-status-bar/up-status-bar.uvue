import { computed } from 'vue';
import defProps from './statusBar';
import { addUnit, addStyle, deepMerge } from '../../libs/function/index';
const __sfc__ = defineComponent({
    __name: 'up-status-bar',
    name: 'up-status-bar',
    props: {
        bgColor: {
            type: String,
            default: 'transparent'
        },
        customStyle: {
            type: Object,
            default: (): UTSJSONObject => ({})
        }
    },
    setup(__props) {
        const __ins = getCurrentInstance()!;
        const _ctx = __ins.proxy as InstanceType<typeof __sfc__>;
        const _cache = __ins.renderCache;
        const props = __props;
        const style = computed<UTSJSONObject>((): UTSJSONObject => {
            const s = { __$originalPosition: new UTSSourceMapPosition("s", "uni_modules/uview-ultra/components/up-status-bar/up-status-bar.uvue", 31, 9), } as UTSJSONObject;
            const statusBarHeight = uni.getSystemInfoSync().statusBarHeight;
            s['height'] = addUnit(statusBarHeight, 'px');
            s['backgroundColor'] = props.bgColor;
            return deepMerge(s, addStyle(props.customStyle));
        });
        return (): any | null => {
            return _cE("view", _uM({
                style: _nS([style.value]),
                class: "up-status-bar"
            }), [
                renderSlot(_ctx.$slots, "default")
            ], 4 /* STYLE */);
        };
    }
});
export default __sfc__;
export type UpStatusBarComponentPublicInstance = InstanceType<typeof __sfc__>;
const GenUniModulesUviewUltraComponentsUpStatusBarUpStatusBarStyles = [_uM([["up-status-bar", _pS(_uM([["width", "100%"]]))]])];
//# sourceMappingURL=up-status-bar.uvue.map