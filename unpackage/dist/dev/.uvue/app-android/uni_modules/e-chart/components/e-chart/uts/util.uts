// 随机生成元素
export function getRandomId(): string {
    return `chart_${Math.random().toString(36).substring(2, 16)}`;
}
// 加rpx单位
export function addUnitRpx(num: number | string): string {
    return typeof num === 'number' ? `${num as number}rpx` : num as string;
}
//# sourceMappingURL=util.uts.map